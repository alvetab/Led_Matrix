package com.example.appled;

//import netscape.javascript.JSObject;

import java.sql.Array;

public class RNNativeApi {
    /*
    public void sendLedProgram(ReadableMap map, Callback callback) {
        String s = map.getString("json");
        int index = map.hasKey("index") ? map.getInt("index") : -1;
        MainActivity.addUdpCallback(YsUdpType.SEND_PROGRAM, callback);
        YsUdpLib.fromContext(null).sendLedProgram(s, index, getReactApplicationContext());
    }
    public void sendLedProgram(Array cadena) {

        String s = cadena.getString("json");
        int index = map.hasKey("index") ? map.getInt("index") : -1;
        MainActivity.addUdpCallback(YsUdpType.SEND_PROGRAM, callback);
        YsUdpLib.fromContext(null).sendLedProgram(s, index, getReactApplicationContext());
    }*/

    /*var e = {
            cmd: {
        delete: {
        del_all: 1
        }
        }*/
        }